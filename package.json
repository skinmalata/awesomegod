{
  "name": "awesome-god-ministries",
  "version": "1.0.0",
  "description": "Awesome God Ministries — native mobile app for Android",
  "private": true,
  "scripts": {
    "sync": "cap sync android",
    "apk": "npm run sync && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}